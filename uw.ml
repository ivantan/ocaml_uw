(* our first program *)
(* don't forget the jj
   and loading modules require a hash "#load"
   and the difference between local and global expressions
   expressions are not really variables, to use variables use
   references, as in "let my_ref = ref 0;;" 
*)